# lc workspace — task: {{ task_id }}{% if question_id %} · question #{{ question_id }}{% endif %}{% if difficulty %} · {{ difficulty }}{% endif %}
# Entry point: {{ entry_point }} — keep the name and signature; `lc test` calls it directly.
# Run `lc test` in this folder; `lc ask --case N` for hints on a failing case.

{{ code_body }}
